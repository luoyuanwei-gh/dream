export const setBreadcrumb = (state, value) => {
  state.pageState.breadcrumb = value
}

export const setContentTabs = (state, value) => {
  state.pageState.contentTabs = value
}

export const setCurrentPage = (state, value) => {
  state.pageState.currentPage = value
}
